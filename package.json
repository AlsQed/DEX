{
  "name": "minimal-amm-dex",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "test:flow": "hardhat run scripts/test.js --network hardhat",
    "test:flow:localhost": "hardhat run scripts/test.js --network localhost",
    "deploy": "hardhat run scripts/deploy-localhost.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy-network.js --network sepolia",
    "add-tokens": "hardhat run scripts/add-tokens.js --network localhost",
    "check-balance": "hardhat run scripts/check-balance.js --network localhost",
    "server": "node server/index.js",
    "server:dev": "nodemon server/index.js"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.2",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.10",
    "nodemon": "^3.0.2"
  }
}

